---
import "@fontsource/noto-sans-lao/500.css";
import "@fontsource/noto-sans-lao/600.css";
import "@fontsource/noto-sans-lao/700.css";
import "@fontsource/roboto-mono/300.css";
import "@fontsource/roboto-mono/400.css";
import "@fontsource/roboto-mono/500.css";

import "../styles/cactus4.css";

import Header from "../components/Header/Header.astro";
const today = new Date();

interface Props {
  title: string;
  description: string;
  img: string;
}

const { title, description, img } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <meta name="generator" content={Astro.generator} />

    <title>{title}</title>
    <meta name="title" content={title} />
    <meta name="description" content={description} />

    <meta property="og:type" content="website" />
    <meta property="og:url" content={Astro.url.href} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={img} />

    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={Astro.url.href} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={img} />
  </head>
  <body>
    <Header />
    <slot />
    <footer
      class="flex f-col center middle container txt-center"
      style="font-size: smaller;"
    >
      <div>{today.getFullYear()}</div>
      <div>
        Web Hecha con Astro y React por <a
          href="https://arjmath.vercel.app/"
          target="_blank"
          rel="noopener noreferrer"
          style="font-size: small; color: inherit">JoseArjona</a
        >
      </div>
      <div>
        Los Recursos son gratuitos y de uso libre. Sin embargo, están limitados
        por el uso de APIS Publicas.
      </div>
    </footer>
  </body>
</html>
